package practica4iterador_pilacola;

/**
 *
 * @author Jonathan
 */
public class Practica4Iterador_PilaCola {

    public static void main(String[] args) {
        /*Agregado AgCola = new ColaConcreta();
        Agregado AgPila = new PilaConcreta();
        Iterador IteCola = AgCola.crearIterador();
        Iterador ItePila = AgPila.crearIterador();

        for (int i = 1; i <= 20; i++) {
            AgCola.Agregar(i);
            AgPila.Agregar(i);
        }

        while (IteCola.tieneSiguiente()) {
            System.out.print(" " + IteCola.Siguiente());
        }

        System.out.println("");

        while (ItePila.tieneSiguiente()) {
            System.out.print(" " + ItePila.Siguiente());
        }

        System.out.println("");*/
    }

}
